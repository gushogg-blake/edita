/*
accepts a document, a codex find result, and an array of
{lineTuples, placeholders} objects describing the replacement,
and returns an edit that will perform the replacement
*/

module.exports = function(document, result, replaceWithLineTuplesWithPlaceholders) {
	
}
